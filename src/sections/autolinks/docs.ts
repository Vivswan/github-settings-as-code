import type { SectionDocs } from "../contract/docs.js";

export const docs: SectionDocs = {
  readme: {
    endpoints: "autolinks CRUD",
    notes: "immutable upstream, so changed entries are replaced",
  },
};
